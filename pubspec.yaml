name: zpl_kit
description: "A declarative layout engine for generating Zebra Programming Language (ZPL) strings with Flexbox-like components."
version: 0.0.2
repository: https://github.com/farookeei/zpl_kit
homepage: https://github.com/farookeei/zpl_kit


environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  # uses-material-design: false 
